# Copilot Instructions

Use `.github/AGENTS.md` as the canonical project playbook.

This repository is a static bilingual D.El.Ed learning website. Keep all changes within plain HTML, CSS, and JavaScript unless the project owner explicitly asks for something else.

Before editing, read `README.md`, `assets/js/catalog.js`, and the relevant agent or skill files under `.github/`.
